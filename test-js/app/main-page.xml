<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="onNavigatingTo">
    <Page.actionBar>
        <ActionBar class="action-bar">
            <ActionBar.titleView>
                <StackLayout class="action-title">
                    <Label text="Your App" />
                </StackLayout>
            </ActionBar.titleView>
        </ActionBar>
    </Page.actionBar>
    <StackLayout class="p-20">
        <Button text="TAP" automationText="tapButton" tap="{{ onTap }}" class="btn btn-primary btn-active" />
        <TextField text="{{ message }}" automationText="message" class="h2 text-center" textWrap="true"/>
    </StackLayout>
</Page>
